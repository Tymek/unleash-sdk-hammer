{
	"name": "unleash-sdk-hammer",
	"description": "when you need some metrics, and every flag looks like a nail",
	"module": "index.ts",
	"type": "module",
	"scripts": {
		"setup": "bun run src/setup.ts",
		"start": "bun run src/server.ts",
		"teardown": "bun run src/teardown.ts"
	},
	"dependencies": {
		"nanoid": "^5.0.7",
		"unleash-client": "^6.1.1"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.8.3",
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5.0.0"
	}
}
